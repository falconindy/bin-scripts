#! /bin/bash

REPOPATH=$HOME

find $REPOPATH -type d -name .git | sed -e 's/.git$//g' | while read repo; do
	cd $repo
	if [[ $(git status | grep -e 'new file: ' -e 'deleted: ' -e 'renamed: ' -e 'modified: ' | wc -l) -gt 0 ]]; then
		echo "$repo"
	fi
done

